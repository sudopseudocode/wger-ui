services:
  db:
    image: postgres:latest
    restart: always
    container_name: wger-db
    env_file: .env.local
    ports:
      - "5432:5432"
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
      - ./scripts/dbscripts:/docker-entrypoint-initdb.d
